/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: false, // Set to false if using pages directory
  },
  trailingSlash: false,
}

module.exports = nextConfig
